server:
  port: 5040
spring:
  application:
    name: api-gateway
  cloud:
    config:
      enabled: false
    bus:
      enabled: false
eureka:
  client:
    service-url:
      defaultZone: http://${ipAddress}:8761/eureka/
